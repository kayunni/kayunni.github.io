---
layout:     post
title:      "[BOJ] 백준 #19532"
subtitle:   "Python 풀이"
date:       2025-01-30 19:00:00
author:     "Kayunni"
# header-img: "img/post-bg-2015.jpg"
catalog: true
tags:
    - Algorithm
---

# 백준 19532

## 문제

수현이는 4차 산업혁명 시대에 살고 있는 중학생이다. 코로나 19로 인해, 수현이는 버추얼 학교로 버추얼 출석해 버추얼 강의를 듣고 있다. 수현이의 버추얼 선생님은 문자가 2개인 연립방정식을 해결하는 방법에 대해 강의하고, 다음과 같은 문제를 숙제로 냈다.

다음 연립방정식에서 *x*와 *y*의 값을 계산하시오.

$$
\begin{cases}
ax + by = c \\
dx + ey = f
\end{cases}
$$

4차 산업혁명 시대에 숙제나 하고 앉아있는 것보다 버추얼 친구들을 만나러 가는 게 더 가치있는 일이라고 생각했던 수현이는 이런 연립방정식을 풀 시간이 없었다. 다행히도, 버추얼 강의의 숙제 제출은 인터넷 창의 빈 칸에 수들을 입력하는 식이다. 각 칸에는 $-999$ 이상 $999$ 이하의 정수만 입력할 수 있다. 수현이가 버추얼 친구들을 만나러 버추얼 세계로 떠날 수 있게 도와주자.

## 입력

정수 *a*, *b*, *c*, *d*, *e*, *f*가 공백으로 구분되어 차례대로 주어진다. (−999≤*a*,*b*,*c*,*d*,*e*,*f*≤999)

문제에서 언급한 방정식을 만족하는 (*x*,*y*)가 유일하게 존재하고, 이 때 −999≤*x*,*y*≤999 의 정수인 경우만 입력으로 주어짐이 보장된다.

## 출력

문제의 답인 *x*와 *y*를 공백으로 구분해 출력한다.

## 풀이 1

```python
# 가장 보편적인 이중 loop
a, b, c, d, e, f = map(int, input().split())
for x in range(-999, 1000):
    for y in range(-999, 1000):
        if a * x + b * y == c and d * x + e * y == f:
            print(x, y)
```

첫번째 풀이는 가장 일반적이라고 생각하는 이중 loop를 활용한 풀이이다. x, y 모두 -999 이상 999 이하의 범위일 때 주어진 두 방정식을 만족시킨다면 해당되는 x, y 값을 한 줄에 출력하도록 하였다.

## 풀이 2

```python
from itertools import product

a, b, c, d, e, f = map(int, input().split())
for x, y in product(range(-999,1000), repeat = 2): # repeat=2의 의미는 같은 범위를 두번 반복하여 x, y 조합을 생성한다는 것
    if a * x + b * y == c and d * x + e * y == f:
            print(x, y)
```

다음은 두번째 풀이이다. 물론 연립방정식 자체를 풀어서 x와 y의 값을 a, b, c, d, e, f 로 나타낸다면 바로 연립방정식의 해를 구할 수 있지만, 문제의 분류가 브루트포스 알고리즘이었기 때문에 해당 방식의 풀이만으로 제출해보았다!

```python
for x, y in product(range(-999,1000), repeat = 2)
```

이 부분은 x, y를 같은 범위(-999 이상, 999 이하)에서 가능한 x, y의 모든 조합을 생성한다는 로직이다. x, y의 범위가 같음에도 개별적인 반복문으로 나타내어야 했던 풀이1에 비해 코드 한줄로 두 변수의 모든 조합을 나타낼 수 있어 훨씬 간결해졌다! 

이처럼 product를 활용하는 건 loop가 이중, 삼중 등 더욱 길어지는 경우에 코드를 간결하게 유지할 수 있어 유용할 것 같다. 

## 첨언..

![스크린샷 2025-01-26 오전 12.30.58.png](attachment:7e267c63-30fe-46cd-ad5a-a04805da40e3:스크린샷_2025-01-26_오전_12.30.58.png)

또한, 두번째처럼 푼다고 했을 때 이중반복문 방식에 비해서 무조건 메모리 및 시간의 효율성이 올라가는 것은 아니란 것도 확인해 볼 수 있었다. 윗줄이 풀이 2, 아랫줄이 풀이 1이다. 메모리 및 시간 측면 모두에서 풀이 1(이중반복문 방식)이 더 나은 결과를 내었다. 다른 문제에서 삼중 이상의 반복문이 등장한다면 두가지 풀이를 다시한번 비교해보아야겠다..